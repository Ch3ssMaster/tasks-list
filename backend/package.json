{
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^6.8.4"
  },
  "devDependencies": {
    "@faker-js/faker": "^7.6.0",
    "@faker-js/faker": "^7.6.0",
    "nodemon": "^2.0.20",
    "cross-env": "^7.0.3",
    "jest": "^29.4.3",
    "supertest": "^6.3.3"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "mock": "node scripts/mockTasks.js tasks-list tasks",
    "mock": "node scripts/mockTasks.js tasks-list tasks",
    "test": "cross-env NODE_ENV=test jest --watchAll --no-cache --testTimeout=5000"
  }
}
